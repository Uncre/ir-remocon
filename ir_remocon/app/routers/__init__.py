"""API ルータ群。

各ルータは自分の ``prefix`` を持ち、``main.py`` が ``include_router`` するだけで
組み上がるようにしてある。

ルータには ``try/except`` を書かないこと。リポジトリと ESP32 通信層が投げる
ドメイン例外は ``main.py`` に登録した例外ハンドラが HTTP に変換する。
「例外を握り潰す try/except」を書く場所自体を無くすのが狙い (バグ B の再発防止)。
"""
